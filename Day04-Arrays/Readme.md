# Day 4 — Arrays & Array Basics

Today I learned how arrays work in JavaScript and practiced accessing, updating, adding, removing, searching, and processing array elements.

## Topics Covered

* Creating arrays
* Accessing elements
* Array indexing
* `length`
* Updating elements
* `push()`
* `pop()`
* `shift()`
* `unshift()`
* `includes()`
* `indexOf()`
* `join()`
* `slice()`
* `splice()`
* `reverse()`
* `sort()`
* Looping through arrays
* Arrays with functions

## Practice

* Sum of array elements
* Largest element
* Smallest element
* Even numbers
* Counting even numbers
* Searching elements
* Array insertion
* Array deletion
* Array reversal

## File

`arrays.js`

## Goal

Understand JavaScript arrays and become comfortable with basic array operations before learning advanced array methods.

